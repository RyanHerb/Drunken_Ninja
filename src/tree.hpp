#include <iostream>
#include <cstdlib>
#include <std::list>


class Tree {

  private:
    int label;
    list::Tree children

  public:
    Tree();
    int getLabel();
    void Tree::addChild(Tree child);
    void Tree::removeChild(Tree child);

}